# 参与 Codex SOS

感谢愿意帮助。这个项目首先服务不会编程、不会命令行、也不会 GitHub 的普通 Codex 用户。一个改动如果让他们多填一张表、多看一次警告或多做一次判断，就需要证明它带来的收益足够大。

## 开始前

请先读下面这些文件。

- [`AGENTS.md`](AGENTS.md)
- [`PRIVACY.md`](PRIVACY.md)
- [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)
- [`docs/ACCEPTANCE_MATRIX.md`](docs/ACCEPTANCE_MATRIX.md)

不要从私有项目、真实会话或真实日志复制代码、规则、路径、提示词或数据。所有提交都应当能够只靠本公开仓库解释来源。

## 适合 v0.1 的贡献

- 修复 Windows 启动、截图粘贴、当前窗口截图或本机英文识字问题
- 适配官方体检格式变化，并增加对应虚构 fixture
- 补充经过验证的保守分类或相似度规则
- 修复隐私遮盖遗漏
- 改善断网、限流、体检失败或等待过久时的降级体验
- 减少安装包体积或用户步骤，同时不降低可靠性
- 增加虚构自动测试和可追溯 UI 验收
- 把用户文案改得更短、更明白

v0.1 暂不接受服务器、账号体系、大模型调用、收费 API、语音、跨平台、自动修复、自动登录 GitHub、自动提交 issue 或大型故障知识库。可以先开讨论说明真实用户需求，不要直接扩大范围。

## 开发环境

- Windows 11 x64
- .NET 10 SDK
- PowerShell 7
- 可选的 Inno Setup 6，仅用于构建安装包

普通用户不需要这些工具。应用发布为自带运行环境的 Windows 包。

先在 GitHub 克隆自己的 fork，进入 `Codex-SOS` 文件夹，再运行测试。

```powershell
pwsh ./scripts/test.ps1
```

测试脚本会恢复锁定的依赖、构建解决方案并实际运行自定义测试器。成功输出必须包含具体通过数量。脚本不会启动测试机上的真实 Codex 官方体检。

使用下面的命令构建本地候选包。

```powershell
pwsh ./scripts/build-release.ps1 -Version 0.1.0
```

缺少 Inno Setup 时仍应生成便携目录和 ZIP，并明确标记安装包未构建。脚本不得静默下载工具、创建远程仓库或发布文件。

## 修改规则

1. 先写清用户能少做哪一步，或哪种真实失败会被正确处理。
2. 用最小改动实现，不顺手增加相邻功能。
3. 给每个行为变化增加虚构 fixture 或自动测试。
4. 分类与建议必须说明证据，不得用“已经确定”“100%”或“体检全绿所以没问题”。
5. 处理截图、体检输出、Windows 记录或搜索词时，先过隐私边界测试。
6. 新依赖要在 PR 中说明收益、许可证、体积、离线行为和维护风险。
7. 更新用户可见行为时，同步 README、CHANGELOG 和相关验收条目。

## 测试资料

正式测试只能使用仓库中明确标注的虚构资料。禁止加入下面这些内容。

- 真实用户名、路径、邮箱、公司或项目名
- 真实截图、日志、会话、账号编号或请求编号
- 可用的密钥、token、cookie 或私有 Git 地址
- 构建机自动采集的数据

安全测试里的密钥形状也要使用明确的 `FICTIONAL` 或 `FIXTURE` 标记，并保证不可用。

官方体检测试使用受控假进程。真实官方体检可能读取检查所需状态、联网并维护登录状态，因此不能被 CI 或本地测试偷偷运行。

## 提交 Pull Request

PR 请简短回答下面这些问题。

- 普通用户会感受到什么变化
- 哪个失败场景得到修复
- 使用了哪些虚构测试
- 是否改变数据读取、联网、官方体检、导出或安装包边界
- 新增依赖的许可证与体积影响

至少附上自动测试结果。涉及 UI 时，按验收矩阵提供虚构场景截图和运行回执。修改者自己的截图不等于最终验收，公开候选还需要独立复核。

## 报告问题

普通问题可以开 issue，只写一句发生了什么并附经过遮盖的材料。安全问题请按 [SECURITY.md](SECURITY.md) 私下报告。

项目不会要求你公开 `auth.json`、完整会话、项目代码或未经遮盖的截图。有人提出这类要求时请停止，并提醒维护者核对流程。
