# 隐私说明

Codex SOS 的目标是减少用户找日志、抄版本和整理报错材料的工作，同时尽量少接触私人内容。

这份说明描述 v0.1 的默认行为。自动遮盖只能降低风险，不能保证 100% 找到所有人名、客户名、内部产品名或图片中的私人信息。

## 用户主动提供的内容

用户可以提供下面两项中的任意一项。

- 一张报错截图
- 一句简短描述

截图只在应用内存中用于本机英文识字。Codex SOS 默认不把原图写到磁盘、不上传原图，也不把原图放进可公开报告。关闭或重新开始后，应用不再保留这张图片。

描述会参与本机判断。用于公开问题搜索前，它会先经过遮盖和稳定词提取。搜索请求只包含最多四个稳定错误短语或症状词，不发送完整描述。

## 自动查看的最小信息

Codex SOS 可以自动查看下面这些信息。

- Windows 版本和电脑类型
- 正在运行的 Codex 进程与安全可读的版本信息
- PATH 中明显的 Codex 安装候选，用于判断不同安装根和版本是否并存
- 故障前后短时间内，Windows 应用记录中明确提到 Codex 的崩溃、无响应或错误事件
- OpenAI 官方公开服务状态
- `openai/codex` 的公开 issue

Windows 记录只查询 Application 日志中的事件 1000、1001、1002，最长回看一小时，最多读取 40 条，然后只保留明确提到 Codex 的少量结构化字段。SOS 不扫描其他 Windows 日志。

## Codex 官方体检

Codex SOS 会自动启动本机官方 `codex doctor --json`。它被当作一个独立的官方黑箱使用。

官方体检为了完成自己的检查，可能读取它需要的认证状态和会话元数据，执行联网探测，并在必要时维护登录状态。Codex SOS 不直接打开或枚举这些内容，也不把账号文件、会话文件或项目内容复制出来。

SOS 只接收官方体检的标准输出、标准错误和结束状态。原始内容只存在于进程内存中，不写入日志或导出包。程序只保留一小组允许字段，例如体检状态、Codex 版本、检查项编号、状态、简短说明和建议；这些字段在展示和导出前再次遮盖。

SOS 最多等待 25 秒。超过这个时间，界面停止等待并继续到结果页，本次打开应用后不会再次启动官方体检。已经开始的真实官方体检会自行结束。这样做是为了避免它恰好在维护登录状态时被强行中断。SOS 不会接收这个迟到进程的结果，也不会在后台启动第二次体检。

测试中的假体检与真实账号完全隔离。只有这种虚构假进程可以用于强制结束的破坏测试。

## 明确不读取的内容

Codex SOS 自己不会读取或扫描下面这些内容。

- `auth.json` 或其他账号凭据文件
- 完整会话、聊天、提示词或任务历史
- 用户项目代码或整个项目目录
- 整个 `.codex` 目录
- 浏览器历史、cookie 或保存的密码
- 不相关的 Windows 日志
- Git 配置或私有远程地址
- `OPENAI_API_KEY` 或其他 API Key

官方体检自己的内部行为不等于 SOS 直接读取这些内容。两者在程序与公开报告中保持分开。

## 自动联网

正常检查只有两个只读联网用途。

1. 读取 `status.openai.com` 的公开服务状态。
2. 通过 GitHub 公共接口搜索 `openai/codex` issue。

请求使用 GET，不带账号，不要求 GitHub 登录，不自动发表、修改或评论内容。没有请求正文。搜索前会先遮盖路径、邮箱、内部网址、编号和疑似密钥，再删除时间与随机串，只发送少量稳定词。

Codex SOS 不连接 `api.openai.com`，不调用大模型，也不读取 API Key。截图、原始体检输出、Windows 原始事件和完整描述不会被上传。

GitHub 可能记录常规网络信息，例如 IP 地址和请求时间。OpenAI 状态页与 GitHub 有各自的隐私政策。断网或限流时，SOS 会给出一个经过遮盖的浏览器搜索入口，不会把搜索失败解释为“没人遇到”。只有用户主动打开，该页面才会在浏览器中加载。

## 用户主动准备官方反馈

只有用户先进入隐私预览、再主动点击“复制材料并打开官方反馈页”，SOS 才会进行下面两件事。

1. 把经过最后一次遮盖、按官方栏目整理的文字复制到本机剪贴板。
2. 打开固定的 `openai/codex` Codex App Bug 页面。

打开的网址只包含官方模板名称，不包含报告、错误文字、路径或标题。SOS 不会自动登录 GitHub、粘贴内容、点赞、上传附件或提交 issue；页面打开后仍是“尚未发送”。用户需要自己快速检查、按栏目粘贴，并选择 SOS 无法安全判断的账号套餐。GitHub 可能要求登录。

如果已经找到高度相似的问题，界面会建议先打开现有问题，确认现象确实相同后再由用户决定是否点 👍，避免重复提交。高度相似仍只代表用户报告相似，不代表 OpenAI 已确认同一根因。

剪贴板可能被电脑上其他正在运行的软件读取。反馈完成后，用户可以复制一段普通文字覆盖剪贴板。安全漏洞、真实密钥、私密 Support case 材料和未经检查的诊断包不应放进公开 issue。

## 遮盖范围

可公开材料会在组装完成后再经过一次整体检查，至少处理下面这些内容。

- Windows 本机路径和用户名
- 邮箱
- 用户明确提供的姓名、项目名、公司名、客户名或内部产品名
- 私有 Git 地址
- 内部网址和内网地址
- 任务、会话、请求和账号编号
- Bearer 内容、密码、疑似密钥和长随机字符串

报告里只显示“遮住了 2 处路径、1 处邮箱”这类数量，不重复展示原值。

固定规则不可能知道所有私人名称。用户主动选择保存时，应用会显示“我们准备公开这些信息”，让用户快速检查报告。没有用户点击，材料不会写入磁盘或发布到外部平台。

## 本机保存

点击“保存到电脑”会在用户选择的位置写出两份 Markdown 文件。

- 可公开报告
- 隐私复核说明

原截图、官方体检原始输出、Windows 原始事件和私有临时材料不会进入这两份文件。Codex SOS 不自动建立 GitHub issue，也不自动上传或删除保存的报告。可选的“准备官方反馈”按钮只复制文字并打开官方页面，不会替用户发送。保存后的保管和发布由用户决定。

## 发现遗漏

如果发现遮盖遗漏，请不要附上真实密钥、账号文件、完整会话、项目代码或未经遮盖的截图。按照 [SECURITY.md](SECURITY.md) 的方式，用完全虚构的例子说明哪类内容没有被遮住。
